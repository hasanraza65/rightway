<?php
/**
 * Index file for /specialties/ directory.
 * Prevents 403 Forbidden on web servers (e.g. Apache, LiteSpeed)
 * when /specialties/ URL is accessed.
 */
require_once dirname(__DIR__) . '/specialties.php';
