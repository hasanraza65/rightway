<?php
/**
 * Index file for /services/ directory.
 * Prevents 403 Forbidden on web servers (e.g. Apache, LiteSpeed)
 * when /services/ URL is accessed.
 */
require_once dirname(__DIR__) . '/services.php';
